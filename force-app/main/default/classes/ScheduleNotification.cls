global class ScheduleNotification implements Schedulable{
     global void execute(SchedulableContext sc){
         /*SendCustomNotificationBatch batch = new SendCustomNotificationBatch();
         database.executebatch(batch,2000);*/
     }
}